// var = require("index");

test("user submits a formula through a text input", () => {
  expect();
});

test("the formula is given to the algorithm and an accurate result is produced", () => {
  expect();
});

test("the formula is displayed on the page in a pre tag or something", () => {
  expect();
});
